# coding = utf-8
import os


# 项目路径
proPath = os.path.abspath(os.path.dirname(os.getcwd()))

# 保存截图路径
imagePath = os.path.join(proPath, "report", "Image")

# 测试报告路径
reportPath = os.path.join(proPath, "report", "TestReport")

# 日志路径
logPath = os.path.join(proPath, "report", "Log")

